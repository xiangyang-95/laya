__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Nix / direnv
result
result-*
.direnv/
env/
venv/
.venv/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.DS_Store
.env
.pypirc
.idea/


# session transcripts (may contain pasted credentials)
session-*.md
*-session.md
.claude-session*

# laya-ts: exported weights (regenerate via laya-ts/scripts/export_onnx.py)
model-*/
*.onnx
*.onnx.data

# laya-ts: build output + deps
laya-ts/dist/
node_modules/

# uv lockfile (repo pins via pip/venv, not uv)
uv.lock

# agent process docs (specs/plans) - not shipped
docs/superpowers/

# credentials - never commit
.env
.env.*
*.pypirc
.pypirc
*.token
.hf_token
hf_token*
secrets.json
credentials.json

# terminal transcripts dumped by the CLI (they quote whatever was pasted, including tokens)
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*-local-command-*.txt
*-local-command-*.txt

# Documentation site build output (zensical build)
site/

# ruff's cache, and the JSON the research harnesses write on every run
.ruff_cache/
research/*_benchmark_results.json
